# Python 3.10+ required (3.13 may have compatibility issues with some packages)
# Core dependencies
numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.3.0
matplotlib>=3.7.0
seaborn>=0.12.0

# Jupyter and execution
jupyter>=1.0.0
nbconvert>=7.0.0
papermill>=2.5.0

# Causal inference libraries
# Note: For Python 3.13, use older versions. For Python 3.10-3.12, newer versions are available
dowhy>=0.8.0,<0.11.0; python_version>='3.13'
dowhy>=0.11.0; python_version<'3.13'
econml>=0.13.0; python_version>='3.13'
econml>=0.14.0; python_version<'3.13'

# Visualization
# Note: graphviz Python package requires system Graphviz installation
# Windows: Download from https://graphviz.org/download/
# Linux: sudo apt-get install graphviz
# macOS: brew install graphviz

# Causal discovery
causal-learn>=0.1.3
pgmpy>=0.1.21
# NetworkX: DoWhy 0.8 requires d_separated (available in 3.1-3.4), not d_separation (3.5+)
# For Python 3.13 with DoWhy 0.8, we need NetworkX <3.5
networkx>=3.1,<3.5; python_version>='3.13'
networkx>=3.1; python_version<'3.13'
# NOTEARS may not be available for Python 3.13+ (will be skipped if unavailable)
notears>=0.3.0; python_version<'3.13'

# Data download
requests>=2.31.0
kaggle>=1.5.15

# Testing
pytest>=7.4.0
pytest-cov>=4.1.0

# Report generation
reportlab>=4.0.0
markdown>=3.4.0

# Interactive web interface
streamlit>=1.28.0
