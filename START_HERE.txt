================================================================
         CausalBench - GET STARTED IN 60 SECONDS
================================================================

QUICK SETUP:

1. Install dependencies:
   pip install -r requirements.txt

2. Setup all datasets (run once):
   python setup_all_datasets.py

3. Launch the app:
   streamlit run app.py

4. Open browser:
   http://localhost:8501

DONE! All 4 datasets ready to use.

================================================================

WHAT YOU GET:

4 Built-in Datasets (ALL WORKING):
  ✓ IHDP     - 747 rows    (Healthcare study)
  ✓ Twins    - 11,400 rows (Mortality study)
  ✓ LaLonde  - 614 rows    (Job training study)
  ✓ Sachs    - 853 rows    (Protein networks)

4 Sample Datasets (for testing uploads):
  ✓ marketing_campaign.csv  (Email campaign, ATE=$15)
  ✓ medical_trial.csv       (Drug trial, ATE=-3 days)
  ✓ education_study.csv     (Tutoring, ATE=+8 points)
  ✓ job_training.csv        (Training, ATE=+$1800)

================================================================

TRY IT NOW:

Option A - Built-in Dataset (Easy):
  1. Open app (streamlit run app.py)
  2. Select "Built-in Datasets" → "lalonde"
  3. Choose estimators: IPW, PSM, DML
  4. Set sample: 300
  5. Click "Run Causal Analysis"
  6. See results: ATE ≈ +$900-1800 (training increases earnings!)

Option B - Upload Your Data:
  1. Open app
  2. Select "Upload Custom Data"
  3. Upload marketing_campaign.csv
  4. System validates automatically ✓
  5. Auto-detects columns
  6. Run analysis
  7. Check: ATE ≈ $15 (true value!)

================================================================

WHAT YOU'LL SEE (Example):

Results: Average Treatment Effect (ATE)

Estimator Comparison:
┌───────────┬──────────────┬─────────────┐
│ Estimator │ ATE Estimate │ Runtime (s) │
├───────────┼──────────────┼─────────────┤
│ IPW       │ 898.97       │ 0.08        │
│ PSM       │ 1245.33      │ 0.12        │
│ DML       │ 1087.45      │ 1.50        │
└───────────┴──────────────┴─────────────┘

[Bar Chart]

Interpretation:
Mean ATE: +$1,077

What this means:
- Job training increases earnings by $1,077/year
- All methods agree → HIGH CONFIDENCE ✓
- Positive effect → Training is beneficial

================================================================

TROUBLESHOOTING:

"Command not found"
  → python -m streamlit run app.py

"Module not found"
  → pip install -r requirements.txt

"Datasets show N/A"
  → python setup_all_datasets.py

"Port already in use"
  → streamlit run app.py --server.port 8502

================================================================

FULL DOCS: See README.md

SUPPORT: GitHub Issues

================================================================

YOU'RE READY! Launch the app and explore! 🚀

================================================================
